import Card from '../components/Card';

export default function Settings() {
  return (
    <div className="page-stack">
      <Card title="Settings" subtitle="Configuration controls for future phases.">
        <p className="muted">Settings UI placeholder.</p>
      </Card>
    </div>
  );
}
